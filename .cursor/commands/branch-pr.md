# branch-pr

Create a new branch, commit all the available code, push, create a Pull Request.

When creating the PR, prefer GitHub CLI (`gh`) for automation (requires `gh auth login`
or a configured `GH_TOKEN`). Avoid MCP-based PR creation.

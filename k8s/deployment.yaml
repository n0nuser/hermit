apiVersion: apps/v1
kind: Deployment
metadata:
  name: localrag-api
  namespace: default
  labels:
    app: localrag-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app: localrag-api
  template:
    metadata:
      labels:
        app: localrag-api
    spec:
      containers:
        - name: localrag-api
          image: localrag-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
          envFrom:
            - configMapRef:
                name: localrag-api-config
            - secretRef:
                name: localrag-api-secrets
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          volumeMounts:
            - name: data
              mountPath: /app/data
      volumes:
        - name: data
          emptyDir: {}

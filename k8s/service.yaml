apiVersion: v1
kind: Service
metadata:
  name: hermit-api
  namespace: default
  labels:
    app: hermit-api
spec:
  type: ClusterIP
  selector:
    app: hermit-api
  ports:
    - name: http
      port: 8000
      targetPort: 8000
      protocol: TCP
